# Copy this file to .env and fill in your values.
# Never commit .env to source control.

# Supabase project URL (find in: Supabase Dashboard → Project Settings → API)
VITE_SUPABASE_URL=https://oshzvamtckpptdjkqdvl.supabase.co

# Supabase anon/public key (safe to expose in browser bundles)
VITE_SUPABASE_ANON_KEY=

# Backend API base URL (Railway, Fly.io, or local)
# Leave blank to default to http://localhost:8000 during development
VITE_API_BASE_URL=
